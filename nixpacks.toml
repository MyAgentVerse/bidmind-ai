[phases.setup]
aptPkgs = ["gcc", "libpq-dev"]

[start]
cmd = "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --timeout-keep-alive 600 --timeout-graceful-shutdown 60"
